# Week 1 walking skeleton — keep lean, add as needed
requests>=2.32
pydantic>=2.7
python-dotenv>=1.0
psycopg[binary]>=3.2
sentence-transformers>=5.0
anthropic>=0.116
langgraph>=0.2

# Later (uncomment when wiring the API / observability)
# fastapi>=0.115
# uvicorn>=0.30
# langgraph>=0.2
# anthropic>=0.40
# langfuse>=2.50
